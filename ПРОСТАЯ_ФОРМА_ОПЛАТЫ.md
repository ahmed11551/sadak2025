# 📝 Простая форма оплаты для Sadaka-Pass

## ✅ Что реализовано

Создана простая форма для заявок на пожертвование **БЕЗ реальной оплаты**. Пользователи могут оставить заявку, которая будет обработана вручную.

### Форма содержит:
- ✅ Имя пользователя
- ✅ Телефон (обязательное поле)
- ✅ Email (необязательно)
- ✅ Сумма пожертвования (₽)
- ✅ Быстрые кнопки сумм: 100₽, 500₽, 1000₽, 3000₽, 5000₽
- ✅ Назначение пожертвования (необязательно)

## 🎨 Дизайн

- Современный UI с градиентами
- Валидация всех полей
- Сообщение об успешной отправке
- Адаптивная верстка для Telegram

## 🔧 Как это работает

### Frontend (`/donation/:fundId`)
1. Пользователь заполняет форму
2. При отправке вызывается API: `/api/v1/donations/simple-request`
3. Данные отправляются на backend
4. Показывается экран успеха

### Backend API
**Endpoint:** `POST /api/v1/donations/simple-request`

**Схема данных:**
```json
{
  "name": "Иван Иванов",
  "phone": "+7 999 123-45-67",
  "email": "ivan@example.com",  // опционально
  "amount": "1000",
  "currency": "RUB",
  "fund_id": 1,  // опционально
  "purpose": "На лечение детей",  // опционально
  "message": "Дополнительная информация"  // опционально
}
```

**Ответ:**
```json
{
  "success": true,
  "message": "Заявка на пожертвование успешно отправлена",
  "request_id": "REQ-1698412800.123",
  "data": {
    "name": "Иван Иванов",
    "phone": "+7 999 123-45-67",
    "amount": 1000.0,
    "currency": "RUB",
    "purpose": "На лечение детей"
  }
}
```

## 🚀 Как использовать

### 1. Запустить проект
```bash
docker-compose up -d
```

### 2. Открыть форму пожертвования
```
http://localhost:3000/donation
```

Или с указанием фонда:
```
http://localhost:3000/donation/1
```

### 3. Протестировать API через Swagger
```
http://localhost:8000/api/docs
```

## 📋 Что дальше

### Для интеграции с реальной кассой:
1. Получить токены/ключи от платежной системы (YooKassa, CloudPayments, и т.д.)
2. Добавить метод оплаты в `donationsApi`
3. Добавить обработку webhook'ов
4. Обновить форму для перенаправления на оплату
5. Добавить проверку статуса платежа

### Пока что:
- ✅ Форма сохраняет заявки
- ✅ Показывает успешное сообщение
- ✅ Валидация всех полей
- ⚠️ Реальная оплата не интегрирована (по требованию заказчика)

## 🧪 Тестирование

1. Запустить backend: `docker-compose up -d backend`
2. Открыть форму: `http://localhost:3000/donation`
3. Заполнить форму и отправить
4. Проверить в консоли браузера (F12) что запрос ушел

## 📝 Примечания

- Форма работает без авторизации
- Email не обязателен
- Сумма должна быть минимум 1 рубль
- Все заявки логируются в консоль

